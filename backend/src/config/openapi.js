/**
 * OpenAPI 3.0 spec for Prepayment Reconciliation Platform API.
 * Served at /api/api-docs via swagger-ui-express.
 * Spec is loaded from openapi.json to avoid JS object literal parse issues.
 */

import { createRequire } from "module";

const require = createRequire(import.meta.url);
const spec = require("./openapi.json");

export default spec;
