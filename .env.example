# Optional: use for local dev or override in Vercel Project Settings → Environment Variables
# For single Vercel deploy (frontend + API together), set these in Vercel; CORS can stay default.

# Backend (API) – used by serverless function
NODE_ENV=production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d
# Allow frontend origin (use your Vercel URL in production, or * for previews)
CORS_ORIGIN=https://your-app.vercel.app

# Frontend (only if you deploy frontend and backend as separate projects)
# VITE_API_URL=https://your-api.vercel.app
